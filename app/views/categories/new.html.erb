<h1 class="center">Nowa Kategoria</h1>
<%= render 'shared/errors', obj: @category %>
<div class="row">
<%= form_for @category, html: {class: 'form-horizontal'} do |c| %>
    <div class="form-group">
      <div class="control-label col-sm-2">
        <%= c.label 'Nazwa' %>
      </div>
      <div class="col-sm-8">
        <%= c.text_field :name, class: 'form-control', placeholder: 'Nazwa klasy', autofocus: true %>
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-10 col-sm-offset-2">
        <%= c.submit 'Utwórz', class: 'btn btn-primary btn-lg' %>
      </div>
    </div>
<% end %>
</div>